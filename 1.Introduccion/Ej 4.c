include //directiva de precompilacion
int //tipo de dato
main //sentencia compuesta
x, aux, s, d, b, c; //identificador de variable
c=0; //sentencia simple
printf("\n ingrese natural"); //sentencia simple
scanf //Sentencia simple
while (x>0) //Sentencia compuesta
/*Componente de ingreso condicionado*/ //comentario
{d=x%10; s=s+d; x=x/10;} //sentencia compuesta
&& //operador logico
== //operador relacional
if (d!=0 && s%d!=0) //Sentencia estructura
else //Sentencia estructura
"\n ingrese natural" //sentencia simple
"%d" //cadena de control
return 0; //sentencia simple
